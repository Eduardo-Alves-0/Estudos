let numero = 10;

if (numero > 5) {
  let mensagem = "O número é maior que 5";
  console.log(mensagem); // Isso funciona
}

console.log(mensagem); // Isso vai dar erro, porque "mensagem" só existe dentro do if
