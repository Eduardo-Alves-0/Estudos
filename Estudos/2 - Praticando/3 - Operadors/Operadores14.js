let a = 3; // 3x
let b = 6; // +6
let c = 15; // =15

let x = (c - b) / a;
console.log(`O resuldado de ${a}x + ${b} = ${c} é x = ${x}`);


/*
    a = 3 (porque é o número antes do x)
    b = 6 (porque está somando com o 3x)
    c = 15 (número do outro lado do =)
 */